! I will be using all caps for keywords it is just a convention, like using caps lock with SQL
PROGRAM H_MATH
  ! variable names begining with ijlmn are automatically iterpreted as intagers to remove this ancient feature use the
  ! `implicit none` directive
  IMPLICIT NONE


END PROGRAM H_MATH

