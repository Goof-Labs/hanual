# Nim VM - Nim is very similiar to python and compiles to C, so it's got a speedy runtime.
# You may need to install the nim VS code xtension to see this in wonderful syntax highlighting

# Parse file into various parts (Instruction, const, etc. pools)

# Create area of memory (Maybe)

# Create switch case statement (Massive lost of if, elifs and 1 else (Execption))