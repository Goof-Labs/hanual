use std::io

struct Person {
    name: str
    iq: int
}

fn main() {
    let a = 12
    let b = 1

    if a == b {
        println("a == b")
    } elif a != b {
        println("a != b")
    } else {
        println("impossible")
    }

    let thing = new Person();
    println(thing)
    println(1 + 1)

    for let i=0, <10, ++1 {
        println(i)
    }
}
