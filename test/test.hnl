use std::io

struct person {
    name: str
    iq: int
}

fn main() {
    let a = 12
    let b = 1

    if a == b {
        println("a == b")
    } elif a != b {
        println("a != b")
    } else {
        println("impossible")
    }
}
